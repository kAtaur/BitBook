@model IEnumerable<BitBookMVCApp.Models.ProfilePhoto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProfileImage)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProfileImage)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProfilePhotoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProfilePhotoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProfilePhotoId })
        </td>
    </tr>
}

</table>
